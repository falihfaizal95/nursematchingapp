# Used only by scripts/seed.ts (run from the repo root: `npm run seed`).
# Same Supabase project as mobile/.env.local and web/.env.local — this is
# just the plain, unprefixed pair since this script runs in Node, not in
# either app's client bundle.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
