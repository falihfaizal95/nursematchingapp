// The admin console (Active now / Patients / Caregivers) is rendered by
// AdminShell in the layout as client-side tabs, so this route has no body
// of its own. Kept as a valid route target for "/admin".
export default function AdminPage() {
  return null;
}
